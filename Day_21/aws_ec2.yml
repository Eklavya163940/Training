plugin: aws_ec2
regions:
  - us-east-2
filters:
  instance-state-name:
    - running
  tag:Name: Eklavya
hostnames:
  - dns-name
compose:
  ansible_host: public_dns_name
  ansible_private_key_file: ansible-worker.pem
  ansible_user: ubuntu