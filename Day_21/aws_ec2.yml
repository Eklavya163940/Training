plugin: aws_ec2
regions:
  - us-east-2
filters:
  instance-state-name:
    - running
  tag:Name: Eklavya
hostnames:
  - dns-name
compose:
  ansible_host: public_dns_name
  ansible_ssh_private_key_file: anisble-worker.pem
  ansible_user: ubuntu
